# 个人记账——功能需求清单

> 基于 Electron + Vue3 + Element Plus + SQLite 桌面端记账应用
> 各业务数据持久化至本地 SQLite；渲染进程通过 IPC 调用主进程完成数据读写；写入操作走串行队列；软件配置独立存储；文件资源仅存路径不入库。

---

## 一、基础桌面窗口功能

### 1.1 应用启动动画（Splash Screen）

| 项目 | 内容 |
|------|------|
| **功能点描述** | 应用启动时展示品牌启动动画窗口，提升启动体验，掩盖后台初始化耗时。 |
| **操作步骤说明** | 双击应用图标启动 → 自动展示 Splash 窗口 → 后台完成数据库初始化、IPC 注册、主窗口创建 → 主窗口就绪后自动关闭 Splash（至少展示 1.5 秒）→ 显示主窗口。 |
| **预期效果&验收标准** | ① Splash 窗口无边框、居中显示，背景为橙色渐变，展示应用 Logo + 名称 + 跳动加载点动画；② 启动完成后平滑过渡到主窗口，无白屏闪烁；③ 若启动极快（<1.5s），Splash 至少保留 1.5 秒后再关闭。 |
| **完成状态** | ✅ 已实现 |

### 1.2 主窗口创建与管理

| 项目 | 内容 |
|------|------|
| **功能点描述** | 创建主渲染窗口，包含窗口尺寸约束、无边框样式、图标配置等基本属性。 |
| **操作步骤说明** | 启动完成后自动创建主窗口（1100×700，最小限制 800×500），加载 Vue 前端页面。 |
| **预期效果&验收标准** | ① 窗口标题为"个人记账"，无原生菜单栏和边框，使用自定义标题栏；② 窗口可自由缩放但不可小于 800×500；③ macOS 下所有窗口关闭后不退出应用，Dock 点击可重建窗口。 |
| **完成状态** | ✅ 已实现 |

### 1.3 自定义标题栏

| 项目 | 内容 |
|------|------|
| **功能点描述** | 提供无边框窗口下的自定义标题栏，支持窗口拖拽移动、最小化和关闭操作。 |
| **操作步骤说明** | 鼠标拖拽标题栏区域可移动窗口；点击最小化按钮收缩窗口至任务栏；点击关闭按钮退出应用。 |
| **预期效果&验收标准** | ① 标题栏高度 40px，右侧排列最小化和关闭按钮；② 拖拽区域覆盖标题栏非按钮区域（`-webkit-app-region: drag`）；③ 关闭按钮悬停变红色。 |
| **完成状态** | ⚠️ 可优化（缺少最大化/还原按钮，目前仅最小化和关闭） |

### 1.4 侧边栏导航

| 项目 | 内容 |
|------|------|
| **功能点描述** | 左侧竖排图标式导航栏，支持在仪表盘、账单明细、统计分析、账本管理四个页面间切换。 |
| **操作步骤说明** | 点击左侧导航图标切换对应页面，当前页面图标高亮（橙色背景）。 |
| **预期效果&验收标准** | ① 侧边栏宽度 88px，白色背景，顶部品牌 Logo，中间 4 个导航图标，底部设置图标；② 选中态图标背景色为浅橙（#FFF5E6），文字色橙色（#FF8C00）；③ 页面切换使用 v-if 条件渲染，切换流畅无闪烁。 |
| **完成状态** | ✅ 已实现 |

### 1.5 单实例运行锁

| 项目 | 内容 |
|------|------|
| **功能点描述** | 确保同一时间仅运行一个应用实例，防止数据库文件被多个进程同时操作导致锁定。 |
| **操作步骤说明** | 再次双击桌面图标启动应用时，不会创建新窗口，而是将已有主窗口前置并聚焦。 |
| **预期效果&验收标准** | ① 第二次启动应用时，已有窗口自动恢复（若最小化则还原）并获取焦点；② 不会出现两个主窗口或多进程读写数据库的情况。 |
| **完成状态** | ✅ 已实现 |

### 1.6 开发工具快捷键

| 项目 | 内容 |
|------|------|
| **功能点描述** | 提供开发调试入口，通过快捷键唤起 Chrome DevTools。 |
| **操作步骤说明** | 在应用任意页面按下 Ctrl+Shift+I 唤起开发者工具面板。 |
| **预期效果&验收标准** | 快捷键可正常工作，仅在开发调试场景使用。 |
| **完成状态** | ✅ 已实现 |

### 1.7 外部链接安全处理

| 项目 | 内容 |
|------|------|
| **功能点描述** | 应用内任何 https 链接点击后使用系统默认浏览器打开，防止在 Electron 窗口内加载外部网页。 |
| **操作步骤说明** | 用户在应用内点击 https 链接 → 系统默认浏览器打开该链接。 |
| **预期效果&验收标准** | 外部链接不会在当前 Electron 窗口中加载，始终跳转至系统浏览器。 |
| **完成状态** | ✅ 已实现 |

### 1.8 页面标题锁定

| 项目 | 内容 |
|------|------|
| **功能点描述** | 阻止 HTML `<title>` 标签覆盖 Electron 窗口标题。 |
| **操作步骤说明** | 无需用户操作，系统自动阻止。 |
| **预期效果&验收标准** | 窗口标题始终显示为"个人记账"，不受页面 title 影响。 |
| **完成状态** | ✅ 已实现 |

---

## 二、数据增删改查核心功能

### 2.1 账单记录——创建

| 项目 | 内容 |
|------|------|
| **功能点描述** | 新增一条收支记账记录，包含类型、金额、分类、日期、描述、备注等字段。 |
| **操作步骤说明** | ① 点击"记一笔"按钮（仪表盘/账单页均有入口）→ 弹出记账弹窗；② 选择类型（支出/收入）；③ 输入金额（自动格式化，限制 ≤999999.99）；④ 选择分类（根据类型联动展示对应分类）；⑤ 填写描述（选填）、日期（默认当天）、备注（选填）；⑥ 点击"确认记账"。 |
| **预期效果&验收标准** | ① 弹窗打开时类型默认"支出"，金额输入框自动聚焦；② 金额输入非法字符自动过滤，小数点后最多 2 位，失焦时自动格式化为 2 位小数；③ 分类下拉根据收入/支出类型动态切换选项；④ 提交成功后弹窗关闭，列表自动刷新，月度统计数据同步更新；⑤ 记录自动关联当前选中账本。 |
| **完成状态** | ✅ 已实现 |

### 2.2 账单记录——查询列表（分页+多维筛选）

| 项目 | 内容 |
|------|------|
| **功能点描述** | 分页展示账单列表，支持按日期、收支类型、分类、关键词等多维度筛选。 |
| **操作步骤说明** | ① 进入"账单明细"页面，默认展示当前账本下所有记录；② 通过日期选择器筛选特定日期；③ 点击"全部/支出/收入"切换类型筛选；④ 点击分类胶囊按钮按分类筛选；⑤ 在搜索框输入关键词按描述模糊搜索（输入 300ms 防抖）；⑥ 底部翻页按钮切换页码。 |
| **预期效果&验收标准** | ① 列表展示日期、分类标签（彩色圆点）、描述、金额（支出红色/收入绿色）、操作按钮；② 分类胶囊按钮根据当前类型筛选联动展示（选"支出"时只显示支出分类）；③ 切换类型筛选时自动清除不匹配的分类筛选项；④ 顶部汇总条实时显示当前筛选条件下的支出总额、收入总额、总笔数；⑤ 分页信息显示"共 X 条记录，第 Y/Z 页"；⑥ 无数据时显示"暂无数据"空状态。 |
| **完成状态** | ✅ 已实现 |

### 2.3 账单记录——编辑

| 项目 | 内容 |
|------|------|
| **功能点描述** | 修改已有账单记录的全部字段。 |
| **操作步骤说明** | ① 在账单列表点击某条记录的编辑按钮 → 弹出编辑弹窗；② 弹窗回显当前记录的所有字段值；③ 修改所需字段；④ 点击"保存修改"。 |
| **预期效果&验收标准** | ① 编辑弹窗与新增弹窗共用组件，标题和按钮文案区分（"编辑账单"/"保存修改"）；② 所有字段正确回显；③ 保存成功后列表和统计数据同步刷新。 |
| **完成状态** | ✅ 已实现 |

### 2.4 账单记录——删除

| 项目 | 内容 |
|------|------|
| **功能点描述** | 删除指定账单记录，删除前二次确认。 |
| **操作步骤说明** | ① 点击某条记录的删除按钮 → 弹出确认对话框（显示金额）；② 点击"确定删除"执行删除，点击"取消"放弃操作。 |
| **预期效果&验收标准** | ① 删除确认框使用 Element Plus MessageBox，文案包含待删除金额；② 删除成功后列表和统计数据同步刷新。 |
| **完成状态** | ✅ 已实现 |

### 2.5 收支分类——查看

| 项目 | 内容 |
|------|------|
| **功能点描述** | 查看系统预置和用户自定义的收支分类列表，按支出/收入分组展示。 |
| **操作步骤说明** | ① 在账单页点击"分类管理"按钮 → 弹出分类管理弹窗；② 默认展示"支出分类"Tab，可切换至"收入分类"Tab。 |
| **预期效果&验收标准** | ① 系统预置 10 个支出分类（餐饮、交通、购物、住房、娱乐、医疗、教育、通讯、日用、其他支出）和 6 个收入分类（工资、奖金、兼职、理财、红包、其他收入）；② 每个分类显示名称和彩色圆点标识；③ 分类列表按 sort_order 排序。 |
| **完成状态** | ✅ 已实现 |

### 2.6 收支分类——新增

| 项目 | 内容 |
|------|------|
| **功能点描述** | 在当前 Tab（支出/收入）下新增自定义分类。 |
| **操作步骤说明** | ① 打开分类管理弹窗；② 切换至对应 Tab（支出/收入）；③ 在底部输入框输入新分类名称（限 20 字）；④ 按回车或点击"添加"按钮。 |
| **预期效果&验收标准** | ① 新分类即时出现在列表中，分配自动彩色圆点；② 空名称不可提交；③ 新增后输入框清空，可继续添加。 |
| **完成状态** | ✅ 已实现 |

### 2.7 收支分类——编辑

| 项目 | 内容 |
|------|------|
| **功能点描述** | 修改已有分类的名称（行内编辑模式）。 |
| **操作步骤说明** | ① 在分类列表中点击某分类的编辑按钮 → 该行切换为编辑态（输入框替代文字）；② 修改名称后按回车保存，按 Esc 取消。 |
| **预期效果&验收标准** | ① 编辑态输入框自动聚焦并全选文字；② 保存后分类名称即时更新；③ 空名称不可保存。 |
| **完成状态** | ✅ 已实现 |

### 2.8 收支分类——删除

| 项目 | 内容 |
|------|------|
| **功能点描述** | 删除自定义分类（需关注关联数据影响）。 |
| **操作步骤说明** | 点击分类列表中某分类的删除按钮直接删除。 |
| **预期效果&验收标准** | ① 分类删除后列表即时刷新；② 已有关联账单记录的分类被删除后，历史记录中分类名称可能显示异常（当前无防删保护，建议优化）。 |
| **完成状态** | ⚠️ 可优化（缺少删除前置检查——若已有交易关联此分类应提示不可删除或引导迁移） |

### 2.9 账本——查看

| 项目 | 内容 |
|------|------|
| **功能点描述** | 查看所有账本列表，区分默认账本和用户自建账本。 |
| **操作步骤说明** | 进入"账本管理"页面查看。 |
| **预期效果&验收标准** | ① 默认账本（id=1）以大卡片形式置顶展示，带有"默认"橙色标签；② 其余账本以网格卡片展示（2 列），每张卡片显示名称、描述、总笔数、本月支出；③ 每个账本卡片右上角有菜单按钮（⋮）。 |
| **完成状态** | ✅ 已实现 |

### 2.10 账本——新增

| 项目 | 内容 |
|------|------|
| **功能点描述** | 创建新的记账账本。 |
| **操作步骤说明** | ① 点击"新建账本"按钮或网格末位的"创建新账本"卡片 → 弹出表单弹窗；② 输入账本名称（必填，限 30 字）和描述（选填，限 100 字）；③ 点击"创建"。 |
| **预期效果&验收标准** | ① 空名称时提交按钮禁用；② 创建成功后弹窗关闭，账本网格自动刷新；③ 新账本出现在"其它账本"区域，带弹性入场动画。 |
| **完成状态** | ✅ 已实现 |

### 2.11 账本——编辑

| 项目 | 内容 |
|------|------|
| **功能点描述** | 修改已有账本的名称和描述。 |
| **操作步骤说明** | ① 点击默认账本右侧"编辑"按钮，或通过其他账本卡片菜单中的"编辑"选项 → 弹出表单弹窗；② 修改名称/描述；③ 点击"保存"。 |
| **预期效果&验收标准** | 弹窗回显当前账本信息，保存后列表即时更新。 |
| **完成状态** | ✅ 已实现 |

### 2.12 账本——删除

| 项目 | 内容 |
|------|------|
| **功能点描述** | 删除用户自建账本。 |
| **操作步骤说明** | ① 点击账本卡片右上角菜单按钮 → 选择"删除" → 弹出确认对话框；② 确认后执行删除。 |
| **预期效果&验收标准** | ① 默认账本（id=1）不可删除，尝试删除时提示"默认账本不可删除"；② 其他账本删除需二次确认（显示账本名称）；③ 删除后网格自动刷新，若当前选中账本被删除则需切换至默认账本（需关注）。 |
| **完成状态** | ⚠️ 可优化（删除当前选中账本后，账本切换器状态未自动回退至默认账本） |

---

## 三、业务交互功能

### 3.1 仪表盘——KPI 指标卡片

| 项目 | 内容 |
|------|------|
| **功能点描述** | 在首页仪表盘以 4 个卡片展示本月核心财务指标：本月支出、本月收入、本月结余、储蓄率。 |
| **操作步骤说明** | 进入仪表盘页面自动加载当月数据。 |
| **预期效果&验收标准** | ① 4 个卡片一行排列，各自带有图标、标签、金额数值和环比趋势文案；② 支出卡片金额为橙色，收入为绿色，结余根据正负变色，储蓄率为黄色；③ 卡片悬停有轻微上浮动效；④ 切换账本后数据自动更新。 |
| **完成状态** | ⚠️ 可优化（上月环比趋势当前为硬编码的 +0.0% 和"未达标"，未真正计算上月数据做对比） |

### 3.2 仪表盘——最近账单列表

| 项目 | 内容 |
|------|------|
| **功能点描述** | 在仪表盘展示当前账本最近 6 条记账记录。 |
| **操作步骤说明** | 进入仪表盘自动加载，点击"查看全部"跳转至账单明细页。 |
| **预期效果&验收标准** | ① 每条记录显示分类图标、描述/分类名、日期和金额；② 点击"查看全部"跳转后可在账单明细页查看更多。 |
| **完成状态** | ✅ 已实现 |

### 3.3 仪表盘——消费分布饼图

| 项目 | 内容 |
|------|------|
| **功能点描述** | 以 SVG 环形饼图展示当月支出分类占比。 |
| **操作步骤说明** | 进入仪表盘自动加载当月各分类支出数据，以环形图+图例展示 Top 5 分类。 |
| **预期效果&验收标准** | ① 环形图中心显示本月总支出金额；② 右侧图例显示分类名称和占比百分比；③ 数据为空时显示空状态。 |
| **完成状态** | ✅ 已实现 |

### 3.4 仪表盘——快捷入口

| 项目 | 内容 |
|------|------|
| **功能点描述** | 提供 4 个快捷入口卡片：快速记账、账单分析、账本管理、导入账单。 |
| **操作步骤说明** | 点击对应卡片跳转至目标页面或弹出对应弹窗。 |
| **预期效果&验收标准** | ① 快速记账 → 跳转账单页并弹出新增弹窗；② 账单分析 → 跳转统计分析页；③ 账本管理 → 跳转账本管理页；④ 导入账单 → 跳转账单页并弹出 CSV 导入弹窗。 |
| **完成状态** | ✅ 已实现 |

### 3.5 统计分析——多时间段维度切换

| 项目 | 内容 |
|------|------|
| **功能点描述** | 支持按"全部/月/季/年/自定义"五个时间段查看收支统计数据。 |
| **操作步骤说明** | 进入统计分析页 → 点击顶部时间段切换按钮（全部/月/季/年/自定义）；月/季/年模式下通过下拉框选择具体月份/季度/年份；自定义模式下通过日期范围选择器确定起止日期。 |
| **预期效果&验收标准** | ① 默认选中"季"维度（当前季度）；② 切换时间段后 KPI 卡片标签文案联动变化（如"本月支出"/"本季支出"/"年均支出"）；③ 自定义日期选择器为双日历范围选择（Element Plus daterange）；④ 年份下拉可选前后 5 年共 10 年范围。 |
| **完成状态** | ✅ 已实现 |

### 3.6 统计分析——KPI 指标卡片

| 项目 | 内容 |
|------|------|
| **功能点描述** | 在统计分析页展示 4 个 KPI 指标：期间总/月均支出、期间总/月均收入、日均消费、储蓄率。 |
| **操作步骤说明** | 进入统计分析页自动加载，切换时间段后数据联动更新。 |
| **预期效果&验收标准** | ① 支出、收入指标按所选时间段的月数取平均值（全部时间段默认按 120 个月）；② 日均消费 = 期间总支出 / (月数 × 30)；③ 储蓄率 = (总收入 - 总支出) / 总收入 × 100%（保留一位小数）；④ 数值保留 2 位小数，带 ¥ 前缀。 |
| **完成状态** | ✅ 已实现 |

### 3.7 统计分析——收支趋势折线图

| 项目 | 内容 |
|------|------|
| **功能点描述** | 以 ECharts 折线图展示所选时间段内的收支变化趋势，支持日/月/季三种颗粒度切换。 |
| **操作步骤说明** | ① 图表自动渲染；② 点击颗粒度切换按钮（日/月/季）改变数据聚合粒度；③ 图表支持 tooltip 悬停查看具体数值，支持图例点击显隐。 |
| **预期效果&验收标准** | ① 折线图双线展示（支出橙色、收入绿色），带半透明面积填充；② 颗粒度切换时按日/月/季自动聚合数据（日→逐日、月→逐月合并、季→逐季合并）；③ 时间段切换联动默认颗粒度（全部→月、月→日、季→月、年→月、自定义→日）；④ Y 轴数值超 1000 时以 k 为单位显示；⑤ 无数据时显示空状态。 |
| **完成状态** | ✅ 已实现 |

### 3.8 统计分析——消费分类占比饼图

| 项目 | 内容 |
|------|------|
| **功能点描述** | 以 ECharts 环形饼图展示所选时间段内支出分类的金额占比。 |
| **操作步骤说明** | 图表自动渲染，最多展示 Top 8 分类；悬停查看具体金额和百分比；图例点击可隐藏/显示分类。 |
| **预期效果&验收标准** | ① 环形饼图带圆角边框，中心显示总支出金额；② 最多 8 个分类，色彩区分明显；③ 无支出数据时显示空状态。 |
| **完成状态** | ✅ 已实现 |

### 3.9 统计分析——分类消费排行榜

| 项目 | 内容 |
|------|------|
| **功能点描述** | 以排行榜列表展示所选时间段内各支出分类的消费金额排名和占比。 |
| **操作步骤说明** | 排行榜自动渲染，展示 Top 8 分类，每条含排名序号、分类图标、名称、金额、占比进度条、百分比。 |
| **预期效果&验收标准** | ① Top 3 排名序号高亮（橙色背景）；② 进度条宽度按占比计算，色彩与分类关联；③ 金额右对齐，百分比右对齐；④ 无数据时显示空状态。 |
| **完成状态** | ✅ 已实现 |

### 3.10 多账本切换

| 项目 | 内容 |
|------|------|
| **功能点描述** | 支持在多个账本间切换，每个账本独立管理账单数据。 |
| **操作步骤说明** | ① 点击页面顶部账本切换器下拉 → 展示所有账本列表；② 点击目标账本完成切换；③ 当前选中账本名称显示在切换器中。 |
| **预期效果&验收标准** | ① 账本切换器出现在仪表盘、账单明细、统计分析、账本管理页面；② 切换账本后，当前页面的列表/统计数据自动重新加载对应账本数据；③ 选中账本前有勾选图标；④ 切换成功显示"已切换到「xxx」"提示。 |
| **完成状态** | ✅ 已实现 |

### 3.11 CSV 文件导入账单

| 项目 | 内容 |
|------|------|
| **功能点描述** | 支持从 CSV 文件批量导入账单数据，含数据预览和导入结果反馈。 |
| **操作步骤说明** | ① 点击"导入 CSV"按钮（账单页/仪表盘入口）→ 弹出导入弹窗；② 点击上传区域选择 .csv 文件；③ 系统自动解析并预览前 5 条数据（表格展示日期、分类、描述、金额）；④ 确认无误后点击"开始导入"；⑤ 查看导入结果（成功/失败条数）。 |
| **预期效果&验收标准** | ① 支持标准 CSV 格式，自动处理双引号转义；② CSV 格式要求：日期, 类型(收入/支出), 分类名, 金额, 描述；③ 导入时自动为不存在的分类创建新分类；④ 导入的账单关联当前选中账本；⑤ 显示导入结果（成功 X 条 / 失败 X 条），成功为绿色，有失败则为黄色警告；⑥ 导入完成后列表和统计数据自动刷新；⑦ 上传区域限 .csv 格式，单文件提示最大 10MB。 |
| **完成状态** | ✅ 已实现 |

### 3.12 系统桌面通知

| 项目 | 内容 |
|------|------|
| **功能点描述** | 通过 Electron Notification API 发送系统级桌面通知（通知 IPC 通道已注册）。 |
| **操作步骤说明** | 主进程已注册 `notification:show` IPC 通道，渲染进程可通过调用发送通知。 |
| **预期效果&验收标准** | 系统托盘区域弹出通知，显示标题和正文内容，点击后消失。 |
| **完成状态** | ⚠️ 可优化（通知 IPC 已就绪但无业务触发入口，建议在预算超支、月度总结等场景中实际应用） |

---

## 四、数据备份与管理功能

### 4.1 数据库自动建表与初始化

| 项目 | 内容 |
|------|------|
| **功能点描述** | 应用首次启动时自动创建 SQLite 数据库文件及全部业务表结构（user、category、ledger、transactions），并预置默认账本和 16 个初始分类。 |
| **操作步骤说明** | 无需用户干预，首次启动自动完成。 |
| **预期效果&验收标准** | ① 数据库文件创建在 userData 目录下（开发环境为 data.dev.db，生产环境为 data.db）；② 4 张表自动建好，含外键约束和 CHECK 约束；③ 默认账本（id=1,"默认账本"）和 16 个预置分类自动插入（仅在分类表为空时）。 |
| **完成状态** | ✅ 已实现 |

### 4.2 数据库 WAL 模式

| 项目 | 内容 |
|------|------|
| **功能点描述** | 启用 SQLite WAL（Write-Ahead Logging）模式，优化并发读写性能，减少"database is locked"错误。 |
| **操作步骤说明** | 数据库初始化时自动设置 `PRAGMA journal_mode = WAL`。 |
| **预期效果&验收标准** | 读写并发性能提升，高频写入场景下不会出现数据库锁定。 |
| **完成状态** | ✅ 已实现 |

### 4.3 串行写入队列

| 项目 | 内容 |
|------|------|
| **功能点描述** | 所有数据库写入操作（INSERT/UPDATE/DELETE）通过 p-queue 串行队列处理，concurrency=1，确保同一时间仅执行一个写操作。 |
| **操作步骤说明** | 系统自动管理，用户无感知。 |
| **预期效果&验收标准** | 快速连续操作（如 CSV 批量导入）不会触发"database is locked"错误。 |
| **完成状态** | ✅ 已实现 |

### 4.4 旧版本数据自动迁移

| 项目 | 内容 |
|------|------|
| **功能点描述** | 生产环境下，若安装目录存在旧版本 .db 文件而 userData 下暂无，自动将旧数据库文件复制到 userData，确保升级不丢数据。 |
| **操作步骤说明** | 应用启动时自动检测并执行迁移，迁移完成后记录日志。 |
| **预期效果&验收标准** | 用户升级安装新版本后，历史记账数据完整保留。 |
| **完成状态** | ✅ 已实现 |

### 4.5 数据库表结构自动迁移

| 项目 | 内容 |
|------|------|
| **功能点描述** | 自动处理历史遗留的数据库结构不兼容问题：旧表名 `transaction` 重命名为 `transactions`；缺少 `ledger_id` 字段的表自动添加该字段。 |
| **操作步骤说明** | 数据库初始化时自动检测并执行迁移。 |
| **预期效果&验收标准** | ① 旧版本创建的 transaction 表自动重命名为 transactions；② 缺少 ledger_id 字段的 transactions 表自动添加该字段并设置默认值为 1。 |
| **完成状态** | ✅ 已实现 |

### 4.6 操作日志记录

| 项目 | 内容 |
|------|------|
| **功能点描述** | 使用 winston 日志框架记录应用运行日志，包含所有 SQL 语句和业务异常信息。 |
| **操作步骤说明** | 自动运行。控制台输出实时日志，文件日志存储在 `userData/logs/app.log`。 |
| **预期效果&验收标准** | ① 文件日志单文件最大 5MB，自动轮转保留 5 个归档；② 日志内容包含时间戳、级别、消息，SQL 日志含完整参数；③ 可用于排查问题和审计。 |
| **完成状态** | ✅ 已实现 |

### 4.7 数据备份与恢复

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（尚未实现） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发（建议增加手动导出 JSON/CSV 备份及导入恢复功能） |

### 4.8 数据导出

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（尚未实现） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发（建议支持按时间段导出账单为 Excel/CSV/PDF 格式） |

---

## 五、偏好与系统设置功能

### 5.1 全局主题色

| 项目 | 内容 |
|------|------|
| **功能点描述** | 应用统一使用暖橙色（#FF8C00）作为品牌主色调，通过 Element Plus CSS 变量全局覆盖。 |
| **操作步骤说明** | 系统预设，用户无需操作。 |
| **预期效果&验收标准** | 主按钮、选中态、高亮文字、图表主色等均统一为暖橙色。 |
| **完成状态** | ✅ 已实现 |

### 5.2 毛玻璃磨砂背景样式

| 项目 | 内容 |
|------|------|
| **功能点描述** | 全局卡片/面板采用半透明白色 + backdrop-filter 毛玻璃效果 + 柔和边框的视觉风格。 |
| **操作步骤说明** | 系统预设，所有卡片和面板自动应用。 |
| **预期效果&验收标准** | 卡片背景 `rgba(255,255,255,0.85)` + `backdrop-filter: blur(8px)` + 浅灰边框，底层背景图可见。 |
| **完成状态** | ✅ 已实现 |

### 5.3 应用背景图

| 项目 | 内容 |
|------|------|
| **功能点描述** | 应用整体背景层使用一张背景图片，以 soft-light 混合模式叠加在纯色背景上。 |
| **操作步骤说明** | 系统预设，用户无需操作。 |
| **预期效果&验收标准** | 背景图全屏覆盖，与毛玻璃卡片形成层次感。 |
| **完成状态** | ✅ 已实现 |

### 5.4 设置页面

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（侧边栏底部有设置图标占位，但无实际页面） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发（建议包含：数据存储路径设置、主题切换、语言切换、预算目标设置、通知偏好等） |

### 5.5 深色模式

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（尚未实现） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发 |

### 5.6 预算管理

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（尚未实现，仪表盘储蓄率目标为硬编码"40%"） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发（建议支持设定月度/分类预算，超支时提醒） |

---

## 六、异常与提示功能

### 6.1 全局 Toast 提示

| 项目 | 内容 |
|------|------|
| **功能点描述** | 使用 Element Plus ElMessage 对操作结果进行即时反馈提示。 |
| **操作步骤说明** | 用户执行增删改操作后自动显示结果提示。 |
| **预期效果&验收标准** | ① 成功操作显示绿色成功提示（如"记账成功""保存成功""删除成功"）；② 失败操作显示红色错误提示（含具体错误信息）；③ 提示 3 秒后自动消失。 |
| **完成状态** | ✅ 已实现 |

### 6.2 删除二次确认

| 项目 | 内容 |
|------|------|
| **功能点描述** | 删除账单、账本等数据时弹出确认对话框，防止误操作。 |
| **操作步骤说明** | 点击删除按钮 → 弹出 Element Plus MessageBox 确认框（类型 warning）→ 点击"确定删除/确定"或"取消"。 |
| **预期效果&验收标准** | ① 确认框文案包含被删除对象的标识信息（金额/账本名）；② 账单删除显示金额，账本删除显示名称；③ 取消操作不执行删除。 |
| **完成状态** | ✅ 已实现 |

### 6.3 表单校验提示

| 项目 | 内容 |
|------|------|
| **功能点描述** | 表单提交前对必填字段和数值范围进行前端校验。 |
| **操作步骤说明** | 填写表单 → 提交时自动校验。 |
| **预期效果&验收标准** | ① 记账金额必须 >0 且 ≤999999.99，金额 0 时提交按钮禁用；② 分类、账本、日期必须已选择，否则提交按钮禁用；③ 账本/分类名称不可为空，否则按钮禁用。 |
| **完成状态** | ✅ 已实现 |

### 6.4 IPC 异常捕获与统一返回

| 项目 | 内容 |
|------|------|
| **功能点描述** | 主进程 IPC Controller 层统一 try/catch 捕获异常，返回 `{code: -1, data: null, msg: 错误信息}` 标准格式；Service 层做参数校验并返回统一 ApiResponse。 |
| **操作步骤说明** | 后端自动处理，前端通过 `res.code === 0` 判断成功或显示 `res.msg`。 |
| **预期效果&验收标准** | 任何数据库操作异常都不会导致应用崩溃，前端可展示友好错误信息。 |
| **完成状态** | ✅ 已实现 |

### 6.5 空数据状态

| 项目 | 内容 |
|------|------|
| **功能点描述** | 列表/图表无数据时展示空状态组件。 |
| **操作步骤说明** | 自动判断数据是否为空，空则展示 Element Plus el-empty 组件。 |
| **预期效果&验收标准** | ① 账单列表无数据时显示"暂无数据"；② 折线图/饼图无数据时显示"暂无数据/暂无支出数据"空状态图标；③ 分类管理列表无分类时显示"暂无分类"。 |
| **完成状态** | ✅ 已实现 |

### 6.6 提交按钮加载态

| 项目 | 内容 |
|------|------|
| **功能点描述** | 表单提交期间按钮显示加载文案并禁用，防止重复提交。 |
| **操作步骤说明** | 用户点击提交 → 按钮文案变为"提交中..."且禁用 → 操作完成后恢复。 |
| **预期效果&验收标准** | 提交期间按钮不可重复点击，避免数据重复写入。 |
| **完成状态** | ✅ 已实现 |

---

## 七、打包发布与版本能力

### 7.1 Windows NSIS 安装包

| 项目 | 内容 |
|------|------|
| **功能点描述** | 使用 electron-builder 构建 Windows NSIS 安装包。 |
| **操作步骤说明** | 执行 `npm run package:win` → 先 electron-vite build → 再用 electron-builder 打包为 .exe 安装包，输出至 release 目录。 |
| **预期效果&验收标准** | ① 生成 `个人记账 Setup 1.0.0.exe`，支持自定义安装路径；② 安装后程序可正常启动，桌面创建快捷方式；③ 应用图标为 build/icon.ico。 |
| **完成状态** | ✅ 已实现 |

### 7.2 macOS DMG 安装包

| 项目 | 内容 |
|------|------|
| **功能点描述** | 使用 electron-builder 构建 macOS DMG 安装包，同时支持 x64 和 arm64 架构。 |
| **操作步骤说明** | 执行 `npm run package:mac` → 构建并打包为 .dmg 文件。 |
| **预期效果&验收标准** | 生成的 DMG 在 Intel 和 Apple Silicon Mac 上均可正常安装运行。 |
| **完成状态** | ✅ 已实现 |

### 7.3 应用图标

| 项目 | 内容 |
|------|------|
| **功能点描述** | 配置 Windows（.ico）和 macOS（.icns）平台应用图标。 |
| **操作步骤说明** | 图标资源放置在 build/ 目录，打包时自动引用。 |
| **预期效果&验收标准** | 安装后桌面快捷方式、任务栏、窗口标题栏均显示正确图标。 |
| **完成状态** | ✅ 已实现 |

### 7.4 Windows 通知标识

| 项目 | 内容 |
|------|------|
| **功能点描述** | 生产环境下设置 Windows AppUserModelId，确保系统通知正确关联到应用。 |
| **操作步骤说明** | 应用启动时自动设置 `app.setAppUserModelId('com.electron.personal-finance')`。 |
| **预期效果&验收标准** | Windows 通知中心中通知来源显示为"个人记账"图标和名称。 |
| **完成状态** | ✅ 已实现 |

### 7.5 版本号管理

| 项目 | 内容 |
|------|------|
| **功能点描述** | 通过 package.json version 字段管理应用版本，当前为 1.0.0。 |
| **操作步骤说明** | 更新 package.json 中的 version 字段后重新打包。 |
| **预期效果&验收标准** | 安装包文件名和程序属性中版本号与 package.json 一致。 |
| **完成状态** | ✅ 已实现 |

### 7.6 自动更新

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（尚未实现） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发（建议集成 electron-updater 实现自动检测和下载更新） |

### 7.7 崩溃报告收集

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（尚未实现） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发（建议集成崩溃报告 SDK，便于收集线上异常） |

### 7.8 安装包数字签名

| 项目 | 内容 |
|------|------|
| **功能点描述** | —（尚未实现） |
| **操作步骤说明** | — |
| **预期效果&验收标准** | — |
| **完成状态** | ❌ 待开发（Windows 代码签名证书 + macOS 公证，消除安装时"未知开发者"警告） |

---

## 统计汇总

| 模块 | 已实现 | 可优化 | 待开发 | 合计 |
|------|--------|--------|--------|------|
| 一、基础桌面窗口功能 | 7 | 1 | 0 | 8 |
| 二、数据增删改查核心功能 | 10 | 2 | 0 | 12 |
| 三、业务交互功能 | 10 | 2 | 0 | 12 |
| 四、数据备份与管理功能 | 6 | 0 | 2 | 8 |
| 五、偏好与系统设置功能 | 3 | 0 | 3 | 6 |
| 六、异常与提示功能 | 6 | 0 | 0 | 6 |
| 七、打包发布与版本能力 | 5 | 0 | 3 | 8 |
| **总计** | **47** | **5** | **8** | **60** |

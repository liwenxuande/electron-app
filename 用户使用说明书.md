# 个人记账 — 用户使用说明书

> 轻松记录每一笔收支，管理你的日常账本

---

## 目录

1. [产品介绍](#1-产品介绍)
2. [安装与运行](#2-安装与运行)
3. [界面整体介绍](#3-界面整体介绍)
4. [功能使用教程](#4-功能使用教程)
   - 4.1 仪表盘
   - 4.2 新增一笔账单
   - 4.3 账单明细管理
   - 4.4 CSV 批量导入账单
   - 4.5 统计分析
   - 4.6 分类管理
   - 4.7 账本管理
5. [本地数据存储说明](#5-本地数据存储说明)
6. [常见问题 FAQ](#6-常见问题-faq)
7. [数据备份与恢复](#7-数据备份与恢复)
8. [版本更新说明](#8-版本更新说明)

---

## 1. 产品介绍

### 1.1 这是什么？

「个人记账」是一款运行在电脑上的**本地记账软件**。你可以用它记录每天的收入和支出，查看消费趋势，管理多个账本，让财务状况一目了然。

### 1.2 核心优势

| 优势 | 说明 |
|---|---|
| **完全离线** | 无需联网，没有网络也能正常使用全部功能 |
| **数据本地** | 所有账目都保存在你自己的电脑上，不上传到任何服务器 |
| **隐私安全** | 没有账号登录，没有云端同步，你的财务数据只有你自己能看到 |
| **多账本** | 可以创建多个账本（如"日常开销"、"旅行记账"），分开管理 |
| **智能分类** | 预置 16 个常用收支分类，也支持自定义新增 |
| **可视化统计** | 折线图、饼图、排行榜，直观掌握消费趋势 |
| **CSV 导入** | 支持从 CSV 文件批量导入历史账单，快速迁移数据 |

### 1.3 隐私声明

- 本应用**不会**收集任何个人信息
- 本应用**不会**连接互联网
- 所有数据**仅存储在你的电脑本地**
- 卸载应用后，如未手动备份，数据将随安装目录一同被删除

---

## 2. 安装与运行

### 2.1 Windows 安装

1. 双击安装包文件（`.exe` 格式）
2. 在安装向导中选择安装目录（建议使用默认路径，也可自定义）
3. 点击「安装」，等待安装完成
4. 桌面会出现「个人记账」图标，双击即可启动

### 2.2 macOS 安装

1. 双击安装包文件（`.dmg` 格式）
2. 将「个人记账」图标拖动到「应用程序」文件夹
3. 打开「应用程序」文件夹，双击「个人记账」图标启动

> 首次在 macOS 上打开时，系统可能提示"无法验证开发者"。  
> 解决方法：打开 **系统设置 → 隐私与安全性**，找到被阻止的应用，点击「仍然允许」。

### 2.3 启动体验

启动时会看到一个带有橙色渐变背景和 logo 的启动画面（持续约 1-2 秒），随后自动进入主界面。

---

## 3. 界面整体介绍

应用采用**左侧导航 + 右侧内容**的经典布局：

```
┌──────────────────────────────────────────────┐
│                  标题栏                        │
│          （最小化 / 最大化 / 关闭按钮）          │
├────────┬─────────────────────────────────────┤
│        │                                     │
│  导航   │            内容区域                  │
│  侧边栏  │                                     │
│        │   根据左侧选中的导航，显示对应页面       │
│  4 个   │                                     │
│  功能   │                                     │
│  图标   │                                     │
│        │                                     │
│   ⚙️   │                                     │
├────────┴─────────────────────────────────────┤
```

### 侧边栏导航（从上到下）

| 图标 | 名称 | 功能简介 |
|---|---|---|
| 🏠 | **仪表盘** | 查看本月收支概览、最近账单、消费分布图 |
| 📋 | **账单明细** | 查看、筛选、搜索所有账单记录 |
| 📊 | **统计分析** | 多维度图表分析收支趋势 |
| 📒 | **账本管理** | 创建、切换、编辑、删除账本 |

### 标题栏

- 支持拖拽移动窗口
- 右上角有**最小化**和**关闭**按钮
- 可以通过快捷键 `Ctrl + Shift + I` 打开开发者工具（调试用）

---

## 4. 功能使用教程

### 4.1 仪表盘 — 一目了然的收支概览

仪表盘是打开应用后的第一个页面，提供全局视角。

**页面包含：**

1. **本月概览卡片**（4 张）
   - 本月支出：本月花费的总金额
   - 本月收入：本月收到的总金额
   - 本月结余：收入减去支出
   - 储蓄率：结余占收入的百分比（目标 40%）

2. **最近账单**
   - 显示最近 6 条交易记录
   - 包含分类图标、描述和金额

3. **消费分布图**
   - 环形图展示各分类的支出占比

4. **快捷入口**（底部 4 个按钮）
   - 快速记账：跳转到账单明细并打开新增弹窗
   - 账单分析：跳转到统计分析页面
   - 账本管理：跳转到账本管理页面
   - 导入账单：打开 CSV 导入弹窗

---

### 4.2 新增一笔账单

这是最核心的操作，有两种方式进入：

**方式一：** 通过仪表盘的「快速记账」按钮  
**方式二：** 切换到「账单明细」页面，点击右上角的新增按钮

**新增账单弹窗的操作步骤：**

1. **选择类型**：点击「支出」或「收入」标签切换
2. **输入金额**：在金额框中输入数字（支持小数，如 `12.50`）
3. **选择分类**：在下拉列表中选择一个分类（如"餐饮"、"交通"等）
4. **填写描述**：输入这笔消费的说明（如"午餐 - 牛肉面"）
5. **选择日期**：默认是今天，点击日期选择器可以修改
6. **填写备注**（可选）：添加额外说明
7. 点击「确定」保存

> 编辑已有账单：在账单明细列表中，点击某条记录右侧的「编辑」图标，弹出编辑窗口，修改后保存即可。

---

### 4.3 账单明细管理

账单明细页面用于查看和管理所有交易记录。

#### 页面结构

- **顶部摘要栏**：显示支出总额、收入总额、记录笔数
- **筛选工具栏**：日期、类型、关键词、分类
- **数据表格**：每条记录的详细信息
- **分页控制**：翻页浏览

#### 筛选和搜索

| 筛选项 | 操作方法 |
|---|---|
| **日期范围** | 点击日期选择器，选择起止日期 |
| **交易类型** | 点击「全部 / 支出 / 收入」按钮切换 |
| **关键词搜索** | 在搜索框中输入关键词（如"咖啡"、"工资"），自动匹配描述 |
| **分类筛选** | 点击分类标签（如"餐饮"、"交通"），只显示该分类的记录 |

#### 删除账单

在表格的操作列中，点击「删除」图标，确认后即可删除该记录。

> 删除操作不可撤销，请谨慎操作。

---

### 4.4 CSV 批量导入账单

如果你在其他记账软件中有历史数据，可以导出为 CSV 格式后导入到本应用。

**操作步骤：**

1. 在仪表盘点击「导入账单」，或在账单明细页面点击导入按钮
2. 在弹出窗口中点击「选择文件」，选择 `.csv` 格式文件（最大 10MB）
3. 系统会自动识别文件中的列，显示前 5 条数据预览
4. 确认无误后，点击「开始导入」
5. 导入完成后显示结果：成功导入多少条、跳过多少条

**CSV 文件格式要求：**

系统会根据表头自动识别列，以下关键词可以被识别：

| 列内容 | 可识别的表头关键词 |
|---|---|
| 日期 | 日期、时间、date、time |
| 类型 | 类型、type（支出/收入） |
| 分类 | 分类、category |
| 金额 | 金额、amount |
| 备注/描述 | 备注、描述、说明、note、description |

> 如果 CSV 中没有单独的「类型」列，系统会根据金额的正负自动判断：正数为收入，负数为支出。  
> CSV 中出现的新分类名称会自动创建。

---

### 4.5 统计分析 — 用图表看懂你的消费

统计分析页面提供多种维度的数据可视化。

#### 时间维度切换

页面顶部有 5 种时间范围可选：

| 维度 | 说明 |
|---|---|
| **全部** | 查看所有历史数据的汇总 |
| **月** | 查看某个月份的数据（可选择月份） |
| **季** | 查看某个季度的数据（可选择季度） |
| **年** | 查看某个年份的数据（可选择年份） |
| **自定义** | 自由选择起止日期范围 |

#### 页面包含的图表

**1. KPI 概览卡片**
- 平均支出、平均收入、日均消费、储蓄率

**2. 收支趋势折线图**
- 橙色线 = 支出，绿色线 = 收入
- 支持切换查看粒度：**日 / 月 / 季**
- 鼠标悬停可查看具体数值

**3. 消费分类饼图**
- 环形图展示各分类的支出占比
- 中心显示总支出金额
- 最多展示前 8 个分类，其余归入"其他"

**4. 分类消费排行榜**
- 按消费金额从高到低排列
- 彩色进度条直观展示各分类占比

---

### 4.6 分类管理 — 自定义你的收支分类

预置了 16 个常用分类，你也可以自由新增、编辑、删除。

**打开方式：** 在账单明细页面，点击筛选栏中的「分类管理」按钮。

**分类管理弹窗：**

- 顶部有「支出」和「收入」两个标签页，点击切换
- 列表中显示当前分类名称和图标

**操作：**

| 操作 | 步骤 |
|---|---|
| **新增分类** | 点击顶部的「新增分类」按钮，输入名称，按回车确认 |
| **编辑分类** | 双击分类名称，直接在输入框中修改，按回车保存 |
| **删除分类** | 点击分类行右侧的删除图标，确认后删除 |

> 如果某个分类下已有账单记录，删除后这些记录的分类会显示为"未分类"，不会被删除。

---

### 4.7 账本管理 — 多账本独立记账

你可以创建多个账本，把不同场景的收支分开记录。例如：
- "日常开销" — 记录每天的生活支出
- "旅行记账" — 记录某次旅行的所有花费
- "家庭账本" — 记录家庭共同支出

#### 账本列表

- **默认账本**：每个新用户都有一个"默认账本"，不可删除，带有"默认"徽章
- **其他账本**：你自己创建的账本

#### 账本卡片信息

每张账本卡片显示：
- 账本名称和彩色图标
- 账本描述
- 记录笔数
- 本月支出

#### 操作

| 操作 | 步骤 |
|---|---|
| **切换当前账本** | 点击某张账本卡片，该账本变为当前选中状态（页面顶部的账本切换器也会同步更新） |
| **创建新账本** | 点击虚线边框的「创建新账本」卡片，输入名称和描述，点击确定 |
| **编辑账本** | 点击账本卡片右上角的菜单按钮，选择「编辑」 |
| **删除账本** | 点击菜单按钮，选择「删除」，确认后删除 |

> "默认账本"无法删除，但可以编辑名称和描述。  
> 删除账本后，该账本下的所有账单记录也会一并删除，请谨慎操作。

#### 账本切换器

在仪表盘、账单明细、统计分析页面的标题栏区域，有一个**账本切换下拉菜单**。点击后可以看到所有账本列表，选择一个即可切换到该账本的数据视图。

---

## 5. 本地数据存储说明

### 5.1 数据存放在哪里？

| 操作系统 | 数据文件位置 |
|---|---|
| **Windows** | `C:\Users\<你的用户名>\AppData\Roaming\个人记账\data.db` |
| **macOS** | `~/Library/Application Support/个人记账/data.db` |

> `AppData` 和 `Library` 是隐藏文件夹，在文件管理器中需要开启"显示隐藏文件"才能看到。

### 5.2 数据格式

数据使用 **SQLite** 数据库存储（文件名为 `data.db`），这是一种广泛使用的轻量级数据库格式，无需安装任何额外软件即可使用。

数据库包含以下 4 张数据表：

| 表名 | 内容 | 说明 |
|---|---|---|
| 账本 (ledger) | 账本名称、描述、创建时间 | 至少包含一个"默认账本" |
| 分类 (category) | 分类名称、类型、图标 | 预置 16 个常用分类 |
| 交易记录 (transactions) | 金额、类型、分类、日期、备注 | 核心数据表 |
| 用户 (user) | 用户姓名、电话、地址 | 预留功能，当前未在界面中使用 |

### 5.3 数据安全吗？

- **完全安全**：数据只存在于你的电脑本地，不会发送到任何外部服务器
- **没有网络连接**：应用运行时不需要联网，也不会尝试联网
- **数据库保护**：使用 WAL 模式和串行写入队列，防止数据写入冲突
- **窗口隔离**：应用采用安全架构，确保渲染界面无法直接访问底层数据文件

---

## 6. 常见问题 FAQ

### Q1：应用打不开怎么办？

- 确认操作系统版本满足要求（Windows 10+ / macOS 11+）
- 尝试右键点击应用图标 → 以管理员身份运行
- 如果是 macOS，检查「系统设置 → 隐私与安全性」中是否被阻止

### Q2：数据会丢失吗？

正常情况下不会。数据保存在本地数据库文件中，只要你不手动删除该文件，数据就会一直保留。建议定期备份（见第 7 节）。

### Q3：可以同时在两台电脑上使用吗？

本应用不支持多设备同步。数据仅存储在安装应用的那台电脑上。如需在另一台电脑使用，请先备份数据（导出数据库文件），然后复制到新电脑的对应目录。

### Q4：CSV 导入失败了怎么办？

- 确认文件格式为 `.csv`（不是 `.xlsx` 或 `.xls`）
- 确认文件大小不超过 10MB
- 检查文件的表头行是否包含可识别的关键词（日期、金额等）
- 如果使用了 Excel 导出，建议另存为 UTF-8 编码的 CSV

### Q5：如何查看应用的日志？

应用会自动记录运行日志，存放在数据目录下的 `logs/app.log` 文件中。如果遇到异常问题，可以将此文件提供给技术支持人员。

| 操作系统 | 日志位置 |
|---|---|
| Windows | `C:\Users\<用户名>\AppData\Roaming\个人记账\logs\app.log` |
| macOS | `~/Library/Application Support/个人记账/logs/app.log` |

### Q6：不小心关闭了窗口，数据还在吗？

是的。关闭窗口不会丢失数据，所有数据都已保存在数据库文件中。重新打开应用，数据会自动恢复。

### Q7：如何调整窗口大小？

- 拖拽窗口边缘可以自由调整大小
- 点击标题栏的最大化/还原按钮可以切换全屏和窗口模式
- 窗口最小尺寸为 800 × 500 像素

### Q8：卸载应用后数据还在吗？

**不在。** 卸载应用时，安装目录和相关数据文件都会被删除。如果需要保留数据，请务必在卸载前先备份数据库文件（见第 7 节）。

---

## 7. 数据备份与恢复

### 7.1 如何备份数据？

**手动备份（推荐）：**

1. 打开文件管理器
2. 导航到数据目录：
   - **Windows**：在地址栏输入 `%APPDATA%\个人记账\` 后回车
   - **macOS**：打开 Finder，按 `Command + Shift + G`，输入 `~/Library/Application Support/个人记账/`
3. 找到 `data.db` 文件
4. 复制该文件到安全位置（如 U 盘、移动硬盘、网盘等）

> 建议定期备份，尤其是在卸载应用或升级系统之前。

### 7.2 如何恢复数据？

1. 确保应用已关闭
2. 将备份的 `data.db` 文件复制到数据目录中（覆盖现有文件）
3. 重新打开应用，数据将自动恢复

### 7.3 迁移到新电脑

1. 在旧电脑上备份 `data.db` 文件
2. 在新电脑上安装「个人记账」应用
3. 打开一次应用（让系统创建数据目录），然后关闭
4. 将备份的 `data.db` 复制到新电脑的数据目录中
5. 重新打开应用，所有数据已迁移到新电脑

---

## 8. 版本更新说明

### 如何更新？

当有新版本发布时，下载最新的安装包并重新安装即可。新版本会自动覆盖旧版本，你的数据不会受到影响（因为数据文件存储在独立的系统目录中，与应用安装目录分开）。

### 更新注意事项

- 更新前建议备份 `data.db` 文件（虽然正常更新不会影响数据）
- 更新后首次启动可能会稍慢（数据库自动迁移）
- 如果更新后遇到问题，可以将之前备份的 `data.db` 恢复到数据目录

---

> **温馨提示：**  
> 本应用的所有数据均存储在你的电脑本地，不会上传到任何云端。请妥善保管你的数据备份文件。如果你在使用中遇到任何问题，可以查看日志文件或联系技术支持。
